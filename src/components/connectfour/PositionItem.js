import React, { PureComponent } from 'react'
import PropTypes from 'prop-types'
import './PositionItem.css'

class PositionItem extends PureComponent {
  static propTypes = {
  }

  classNames() {
  }

  render() {
    return(
      <article className="positionbox">
        
      </article>
    )
  }
}

export default PositionItem
